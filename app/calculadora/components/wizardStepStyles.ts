/** Contenedor lateral del banner: arriba en mobile, columna izquierda en desktop (lg+). */
export const WIZARD_BANNER_SLOT_CLASS = "shrink-0 lg:w-2/5 lg:min-h-0 lg:self-stretch";

/** Panel de contenido blanco a la derecha del banner en desktop. */
export const WIZARD_CONTENT_PANEL_CLASS =
  "flex min-h-0 flex-1 flex-col overflow-auto bg-white px-4 py-4 sm:px-6 sm:py-6";
